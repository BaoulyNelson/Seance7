class Product {
  final int id;
  final String title;
  final double price;
  // Ajoutez d'autres attributs selon vos besoins, comme imageUrl, description, etc.

  Product({required this.id, required this.title, required this.price});
}
